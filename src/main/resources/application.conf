ktor {
    development = true

    environment = dev
    environment = ${?KTOR_ENV}

    deployment {
        port = 18080
        port = ${?PORT}
    }

    application {
        modules = [ dev.aloks.ApplicationKt.module ]
    }
}

zeus {
    domain = "zeus.aloks.dev"
    secrets {
        jwt = "sometopsecretcode"
    }
}

db {
    mongo {
        uri = "mongodb+srv://aloks98:alok_mongo01092021@zeus-cluster1.x0eot.mongodb.net"
        db_name = "zeus_db_test"
        db_name = ${?MONGO_DB_NAME}
    }
}

